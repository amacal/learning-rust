[package]
name = "radix"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = { version = "0.9.1", features = ["std"] }
chrono = { version = "0.4.41", features = ["std"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = false
panic = 'abort'
incremental = false
